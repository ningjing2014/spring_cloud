package ${basePackage}.${packageName}.repository;

import ${basePackage}.base.repository.BaseRepository;
import ${basePackage}.${packageName}.model.${modelName};

public interface ${modelName}Repository extends BaseRepository<${modelName}> {

}
